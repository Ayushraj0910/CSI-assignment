# Core (already used by the offline-fallback demo path)
numpy>=1.24
scikit-learn>=1.3
pypdf>=4.0

# Recommended production upgrades (auto-detected; pipeline falls back
# gracefully if these are not installed):
sentence-transformers>=2.6   # neural embeddings + cross-encoder re-ranker
faiss-cpu>=1.7                # fast approximate nearest-neighbor vector index
datasets>=2.18                 # Hugging Face dataset ingestion (e.g. open_ragbench)
rank-bm25>=0.2.2               # optional: drop-in BM25 if you prefer it over the built-in one

# Optional LLM backends (pick one, set the matching API key env var)
anthropic>=0.34                # ANTHROPIC_API_KEY
openai>=1.30                   # OPENAI_API_KEY
